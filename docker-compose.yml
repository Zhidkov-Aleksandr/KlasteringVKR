services:
  app:
    build: .
    ports:
      - "8501:8501" # Пробрасываем порт 8502 на хост, так как 8501 занят или заблокирован
    volumes:
      - .:/app
      - /app/.venv
    environment:
      - PYTHONUNBUFFERED=1
    command: streamlit run app.py --server.port=8501 --server.address=0.0.0.0